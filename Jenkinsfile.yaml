pipeline:
  agent: any

  environment:
    APP_NAME: "test"

  stages:
    - stage: "Test Run"
      steps:
        script: |
          sh '''
            echo "Running tests..."
            echo "Current date: $(date)"
            echo "Workspace files:"
            ls -la
            echo "Environment variables:"
            echo "APP_NAME = $APP_NAME"
          '''

    - stage: "Run Docker Container"
      steps:
        script: |
          sh '''
            echo "Running Docker container..."
            docker run -d debian:latest sleep infinity
          '''